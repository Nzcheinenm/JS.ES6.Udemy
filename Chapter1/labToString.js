/**
 * СТРОКИ.
 */

let value = 'some test string';

// value = value[0];
// value = value[value.length - 1];

// value = value[0].toUpperCase() + value.slice(1, value.length - 1) +
//     value[value.length - 1].toUpperCase();

// value = value.indexOf('string');

// value = value.indexOf(' ', 5);

// value = value.substr(5, 4);

// value = value.slice(5, 9);

// value = value.slice(0, -6);
let a = 20,
    b = 16;

value = a + "" + b;

console.log(value);